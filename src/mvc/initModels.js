const Registers = require("./registers/registers.model")
const Stations = require("./stations/stations.model")

const initModels = () => {
    // Stations.hasMany(Registers)
    // Registers.hasOne(Stations)
}

module.exports = initModels